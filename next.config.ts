import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["framerusercontent.com", "lh3.googleusercontent.com", "assets.aceternity.com"],
  },
};

export default nextConfig;
